CREATE TABLE emp01
(
  emp_no NUMBER(5),
  emp_name VARCHAR(20),
  emp_sal NUMBER(10) 
)
PARTITION BY RANGE(emp_no)
(
 PARTITION emp_num1 VALUES LESS THAN(102),
 PARTITION emp_num2 VALUES LESS THAN(104),
 PARTITION emp_num3 VALUES LESS THAN(106)
);

SELECT TABLE_NAME, PARTITION_NAME FROM USER_TAB_PARTITIONS WHERE TABLESPACE_NAME='USERS';

SELECT * FROM emp01 PARTITION(emp_num1);

SELECT * FROM emp01 PARTITION(emp_num2);

SELECT * FROM emp01 PARTITION(emp_num3);